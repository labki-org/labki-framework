name: test

on:
  pull_request:
    branches: [ main ]

jobs:
  placeholder-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify repo structure
        run: |
          test -f Dockerfile
          test -f docker-compose.yml
          test -d scripts
          test -d config


